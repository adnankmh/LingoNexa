name: Legacy Dart Workflow - Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the Flutter workflow instead
        run: echo "LingoNexa is a Flutter project. Run 'Flutter CI - APK - AAB - Web'."

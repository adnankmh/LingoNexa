# Workflow rule

Keep `build.yml` as the only `.yml` or `.yaml` file in this directory.

The project is Flutter, not a standalone Dart package. Do not create GitHub's
starter `dart.yml` workflow. Dependency resolution must use `flutter pub get`.

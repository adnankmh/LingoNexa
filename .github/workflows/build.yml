name: Legacy Combined Workflow - Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the separated workflows
        run: echo "Use Flutter CI, APK, AAB, Web, or Deploy GitHub Pages."

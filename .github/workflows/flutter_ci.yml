name: Flutter CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: flutter-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: stable
          cache: true
      - name: Verify Flutter SDK
        run: flutter --version
      - name: Prepare Flutter project
        run: bash scripts/ci_prepare.sh
      - name: Normalize Dart sources
        run: dart format --output=none lib test tool
      - name: Verify multilingual academy
        run: dart run tool/verify_academy.dart
      - name: Verify content and icon guidance contracts
        run: dart run tool/verify_release.dart
      - name: Analyze Flutter project
        run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - name: Run Flutter tests
        run: flutter test
